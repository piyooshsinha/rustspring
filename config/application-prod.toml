# Active when APP_PROFILE=prod.
# Secrets should come from the environment, not this file:
#   APP_DATABASE__URL=postgres://user:pass@db-host:5432/app

[server]
host = "0.0.0.0"
port = 8080

[database]
url = "postgres://postgres:postgres@db:5432/app"
max_connections = 20
